{
  "name": "kayul-faucet-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network optimism",
    "verify": "npx hardhat verify --network optimism",
    "deploy-staking": "npx hardhat run scripts/deploy-staking.js --network optimism",
    "deploy-swap": "npx hardhat run scripts/deploy-swap.js --network optimism",
    "deploy-strategies": "npx hardhat run scripts/deploy-strategies.js --network optimism"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^1.0.0",
    "hardhat": "^2.17.0",
    "hardhat-gas-reporter": "^1.0.9"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.2"
  }
}